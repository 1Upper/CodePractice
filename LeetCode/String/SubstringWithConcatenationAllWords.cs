/*30. Substring with Concatenation of All Words  QuestionEditorial Solution  My Submissions

You are given a string, s, and a list of words, words, that are all of the same length.Find all starting indices of substring(s) in s that is a concatenation of each word in words exactly once and without any intervening characters.

For example, given:
s: "barfoothefoobarman"
words: ["foo", "bar"]

You should return the indices: [0,9].
(order does not matter).

 https://leetcode.com/problems/substring-with-concatenation-of-all-words/
 */
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace LeetCode
{
    class SubstringWithConcatenationAllWords
    {

        public void GetStringCollection(string[] words, string dest, IList<string> collection )
        {
            if (words.Length == 0)
                return;

            for (int i = 0; i < words.Length; i++)
            {

            }
        }

        public IList<int> FindSubstring(string s, string[] words)
        {
            Dictionary<>
        }
    }
}
